let age = Number("24")
console.log(age, typeof age)

age = String(age)
console.log(age, typeof age)

age = Boolean(age) // Boolean returns true if the string is populated
console.log(age, typeof age)
